# The frontend assets are architecture-independent and are built on the host
# before cross is invoked. build.rs then embeds the existing frontend/dist
# files without requiring Node.js inside the cross container.
[build]
default-target = "x86_64-unknown-linux-musl"

[build.env]
passthrough = ["PIXHELF_SKIP_FRONTEND_BUILD=1"]
