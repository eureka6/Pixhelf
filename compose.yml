services:
  gallery:
    image: eureka6688/pixhelf:latest
    container_name: gallery
    restart: unless-stopped
    ports:
      - "3002:3002"
    volumes:
      - "./pictures:/pictures:ro"
      - gallery-data:/data

volumes:
  gallery-data:
    name: gallery-data
